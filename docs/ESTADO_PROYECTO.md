# Estado del proyecto — Taller 2: Dashboard Analítico
**Última actualización:** 2026-05-29 · **Actualizado por:** Claude Code (**Fase III DESPLEGADA** en Streamlit Community Cloud → https://taller02dataviz.streamlit.app/ ; proyecto movido al repo propio del usuario `ylondonq/taller_visualiza_datos-` (remoto `origin`; el del equipo `HeiderZapata` quedó como remoto `equipo`); **refactor de memoria hecho**: el dashboard ya NO carga el clickstream crudo, sino agregados precomputados (~3,9 MB), bajando el pico de RAM de >1 GB a ~194 MB para no caerse en el tier gratuito; **rediseño BI "E-commerce SA" hecho** (sistema de diseño + `app/ui.py`, marca, navegación prominente, héroe+soporte por sección, ayudas no-gráfico, un solo acento rojo, cross-filter estilo Power BI; pico ~214 MB, 45/45); **siguiente: iterar diseño con feedback del equipo**)
**Documentos hermanos:** `Contexto_Taller_Visualizacion_curado.md` (marco conceptual) · `Estrategia_y_prompts_Taller2.md` (plan y prompts)

---

## Cómo usar y mantener este documento
- **Al iniciar CUALQUIER chat del proyecto:** lee COMPLETO este documento **y todos los insumos listados abajo** antes de actuar. No basta con las secciones 1–6: el notebook del EDA es la fuente de las cifras y de los problemas metodológicos que estamos corrigiendo.
- **Al cerrar un chat o terminar una fase:** actualiza el tablero (sección 4), marca tareas (sección 6) y añade una entrada en la bitácora (sección 7) con fecha. Cambia el encabezado "Última actualización".
- Mantenlo corto y veraz: refleja lo que REALMENTE está hecho, no lo planeado.

### Insumos del proyecto (leer TODOS al iniciar)
- **`ESTADO_PROYECTO.md`** (este) — punto de entrada: contexto, decisiones, validez de hallazgos, tablero, próxima acción.
- **`Contexto_Taller_Visualizacion_curado.md`** — marco conceptual del curso (exploratorio vs aclaratorio, Data-to-Ink, preatentivos, Gestalt, jerarquía, acto de habla, diseño de dashboard).
- **`Estrategia_y_prompts_Taller2.md`** — plan por fases y prompts. Se sigue ese orden; no se adelantan fases sin que el usuario lo pida.
- **`taller_2.pdf`** — export del notebook del EDA del equipo (formato real: `.zip` con HTML/imágenes, 28 páginas). Contiene el pipeline reutilizable (limpieza, feature engineering) y los 7 hallazgos originales con su código. **Es la fuente de verdad de lo que hay HOY que estamos corrigiendo.**
- **`02_Contendio_curso_sum.md`** — ⚠️ **DUPLICADO exacto** de `Contexto_Taller_Visualizacion_curado.md`. Redundante; no tratar como fuente distinta.
- **(Tras Fase 0)** `muestra_usuarios.parquet` — nueva muestra por usuario.

---

## 1. Contexto (resumen)
- **Entregable:** dashboard interactivo (Streamlit) que responda preguntas de negocio estratégicas, documentando la transición de Análisis Exploratorio → Composición del Mensaje (Aclaratorio + Storytelling).
- **Rúbrica:** EDA 30% · Storytelling/Aclaratorio 35% · Dashboard 35%. **Criterio de maestría:** el gerente decide una acción en 30 segundos.
- **Problema:** e-commerce multicategoría; conversión 1–3%; objetivo = identificar a quién/cuándo/cómo incentivar (propensión + uplift). Conecta con el proyecto integrador.
- **Preguntas de negocio (explícitas, las que responde el dashboard):**
  - **PN1 — ¿Dónde perdemos conversión y cuánto vale recuperarla?** → Palanca A (funnel 4.1, abandono 4.5, premio en $ 4.8a, marca 4.8c).
  - **PN2 — ¿Quiénes son los clientes valiosos y cómo retenerlos?** → Palanca B (recurrencia 4.6, timing 4.8b).
  - **PN3 — ¿Cuándo y con qué activar el incentivo?** → hora/intensidad (4.3), velocidad (4.7), precio-no-es-freno (4.2).
- **Datos:** clickstream REES46 (oct 2019). El subset original `muestra_eventos_def.csv` quedó descartado como base (ver decisión D1). El crudo `2019-Oct.csv` (~5GB) está en Google Drive con `FILE_ID = 1kDasOXgXimvPn2Shu3wgZymbqj4_8pDc` (gdown). Volumen de referencia de la muestra actual (para dimensionar la nueva): ~499.212 eventos tras limpieza, 8.607 `purchase`, 10.851 `cart`, 8.340 usuarios compradores.
- **Equipo:** Sara, Yeison, Heider, Kelly + tú.

---

## 2. Decisiones tomadas
- **D1 — Muestreo:** se re-muestrea **por usuario** (cluster sampling) desde el crudo `2019-Oct.csv`, conservando todos los eventos de cada usuario. Motivo: la muestra original era aleatoria por evento y rompía sesiones/usuarios. Preservar usuarios completos sostiene los 7 hallazgos.
- **D2 — Mensaje (FIJADO 2026-05-29; refinado para el test de 30 s):** **una historia, dos momentos de captura, ambos en electrónica** — *"el negocio deja dinero sobre la mesa en electrónica"*. (A) **Antes de comprar:** recuperar carritos abandonados (electronics, Apple/Samsung; $2,08 M en juego), con incentivo inmediato/en pantalla en la mañana. (B) **Después:** retener al núcleo recurrente (31.7% de compradores = 69% del revenue) con nudge de recompra 24–72 h en su misma categoría. Titular único arriba (los $ y la acción); las dos palancas como paneles/tabs. Reemplaza al candidato único "usuario persuadible".
- **D3 — Herramienta del dashboard:** Streamlit/Python.
- **D4 — Estrategia de Fase II / mensaje (FIJADO 2026-05-29):** la rúbrica pide UN gráfico héroe (exploratorio vs aclaratorio) y claridad en 30 s, así que se evita diluir con dos mensajes de igual peso: **un titular único, dos momentos**. **Gráfico héroe recomendado: la Palanca A monetizada** (revenue en juego / abandono de electronics, 4.8a) — es lo más visual, con número de dinero duro y una acción clarísima. Alternativas: slopegraph del funnel (4.1) o intensidad horaria (4.3). El entregable de Fase II es la **comparativa exploratorio→aclaratorio** del héroe + justificación de las decisiones visuales (Data-to-Ink, preatentivos, jerarquía, anotaciones, frase-acción en el título).
- **D5 — No limitarnos a un solo gráfico (FIJADO 2026-05-29, enmienda a D4):** aunque la rúbrica pide "una" comparativa, el checklist exige que **cada gráfica del dashboard** siga Contexto→Hallazgo→Traducción→Acción, y el mensaje D2 es "una historia, **dos** momentos" sobre **tres** PN. Por eso se hará una comparativa exploratorio→aclaratorio **por ancla**: **PN1/Palanca A** = revenue en juego electronics (4.8a, borrador hecho); **PN2/Palanca B** = concentración de valor recurrentes (4.6) + timing recompra (4.8b); **PN3** = intensidad horaria (4.3). El borrador del héroe A (matplotlib) queda como **andamiaje a refinar por el equipo** (pulir estética / migrar a Plotly).

---

## 3. Hallazgos y su validez
| # | Hallazgo | Tipo | Estado |
|---|----------|------|--------|
| 1 | Funnel y conversión por categoría | Global, pero mal definido (ver A1) | ✅ Recalculado con funnel por unidad (4.1). Mensaje corregido: persuadible = electronics/computers, NO construction. Ver §7 |
| 2 | El precio no es el freno | Global | ✅ Recalculado (4.2) con `cat_funnel` + precio por producto único, umbral ≥500. Confirma y refuerza: los más baratos (apparel, accessories) convierten peor. Ver §7 |
| 3 | Ventana horaria global | Marginal | ✅ Re-ejecutado y enriquecido (4.3). Pico real 7h (no 9h), franja 6h–10h; **insight nuevo**: la mañana convierte ~2× por visita (7h: 2.71 vs 16h: 1.20), tráfico pica en tarde pero baja intención. Gráfico de distribución rediseñado (normalizado + intensidad). Ver §7 |
| 4 | Ventana horaria por categoría | Marginal | ✅ Re-ejecutado (4.4). 4/5 categorías pican 6–7h; apparel se desplaza a mediodía (12h, no vespertino); furniture ahora con pico claro (6h). Limitación actualizada a 982.106 eventos (~2.3% de octubre). Ver §7 |
| 5 | Abandono de carrito | Sesión/Unidad | ✅ Recalculado por unidad (4.5) desde el funnel. **Triple contradicción resuelta: abandono real 32.4%** (7.511/23.174), ni 99% (artefacto) ni 79% (sin respaldo). Concentrado en electronics (5.048 = 67% del abandono; +appliances+computers = 83%). apparel cierra 100% (freno upstream). Ver §7 |
| 6 | One-time vs recurrentes | Usuario | ✅ Recalculado por ocasión (4.6). **Flip mayor: el 97.1% one-time era artefacto del muestreo.** Real: 31.7% recurrentes (2.544/8.036), concentran 69% del revenue, ticket ~5× ($1.411 vs $293). Abre palanca de retención complementaria al persuadible. Piso (ventana solo-octubre). Ver §7 |
| 7 | Velocidad de decisión | Sesión/Unidad | ✅ Recalculado por producto-en-sesión (4.7). Mediana **2.2 min** (no 5.3), 77.8% <5min, 91.3% <10min. **Negativos: 0.1%** (vs 27% antes) → el daño era del muestreo. 15.607 unidades limpias. Ver §7 |

---

## 4. Tablero de fases
| Fase | Descripción | Estado | Notas |
|------|-------------|--------|-------|
| 0 | Re-muestreo por usuario (Parquet) | ✅ Hecho | n_users=71.199 (semilla 42); 982.106 eventos, 16.925 purchase. Ver §7 |
| 1 | Re-ejecutar EDA sobre nueva muestra | ✅ Hecho | Recalculadas todas las secciones (1–3 calidad, 4.1–4.7) + 4.8 análisis de oportunidad + 4.9 síntesis + 4.10 comparativa. Los 7 hallazgos rehechos sobre la muestra por usuario |
| 1b | Reescribir funnel 4.1 (ver A1) | ✅ Hecho | Funnel por unidad (producto-en-sesión) en `src/funnel.py`; usado en 4.1. Ver §7 |
| 2 | Validar/comparar los 7 hallazgos | ✅ Hecho | Tabla comparativa 4.10 (original vs corregido) + 4 análisis de oportunidad. Ver §7 |
| 3 | Seleccionar mensaje central | ✅ Hecho | D2 fijado: dos palancas en paralelo (conversión + retención), ambas en electrónica. Ver §2 |
| 4 | Fase II — Aclaratorio (comparativa) | 🟡 En progreso | Borradores de los héroes A (revenue electronics 4.8a) y B (retención recurrentes 4.6/4.8b): comparativa exploratorio→aclaratorio + justificación en el notebook (FASE II). Pendiente (equipo): pulir estética/Plotly y añadir aclaratoria de PN3 (intensidad horaria 4.3). Ver D5 |
| 5 | Fase III — Dashboard Streamlit | ✅ Desplegada (+ refactor memoria + rediseño BI) | `app/app.py` + `app/theme.py` + `app/ui.py` + `app/charts.py` (Plotly nativo). **Refactor de memoria (2026-05-29):** el app ya NO usa `src/prep.py`+`src/metrics.py` sobre el clickstream crudo (pico >1 GB → caída en la nube); carga **agregados precomputados** (`src/build_aggregates.py` → 5 parquets, ~3,9 MB) vía `src/agg_metrics.py`. **Rediseño BI "E-commerce SA" (2026-05-29):** sistema de diseño (tokens + `app/ui.py`), barra de marca, navegación prominente, jerarquía héroe+soporte por pestaña, ayudas no-gráfico (banner de acción, KPI cards, insight cards CHTA, glosario, callouts), un único acento rojo (sin verde/azul), y cross-filter estilo Power BI sobre los agregados. Pico de RAM ~214 MB (<250). Cifras idénticas al notebook (`src/verify_aggregates.py`, 45/45). Ver §7 |
| 6 | Pitch 3 min + coherencia | ⬜ Pendiente | |

Leyenda: ⬜ pendiente · 🟡 en progreso · ✅ hecho

---

## 5. Ajustes de código del EDA (además del re-muestreo)

(Ver PDF Taller 2 -> es el Notebook ipynb que contiene el EDA)

**A1 — Funnel y conversión por categoría (4.1): REESCRIBIR.**
Hoy calcula cocientes de conteos de eventos (`purchase_events / view_events`), no un funnel de unidades. No clasifica a cada usuario/sesión por su etapa más profunda, así que el denominador (views) está inflado por multi-vistas y no se lee como "% de visitantes que compran". Además `cart_rate` y `conv_rate` cuelgan ambas de views en vez de encadenar (cart→purchase sobre los que llegaron a cart).
- **Confirmado en el código (pág. 11 del notebook):** `conv_df['conv_rate'] = purchases/views` y `conv_df['cart_rate'] = carts/views`; el funnel global izquierdo usa `value_counts()` de `event_type` y `pct = val/views`. Son conteos de eventos, sin encadenar.
- Fix: calcular el funnel a nivel **sesión** (de las sesiones con view, cuántas con cart, y de esas cuántas con purchase) o, ideal, **producto dentro de la sesión** (view P → cart P → purchase P).
- Impacto: afecta Hallazgo 1 y la `conv_rate` que usa el Hallazgo 2.

**A1-bis — Muestreo comentado ≠ ejecutado (dejar claro en el notebook nuevo).**
El notebook tiene un comentario que describía un muestreo estratificado ("100% de purchase/cart + 2% de view"), pero el código realmente ejecutado fue `USING SAMPLE 500000` = **uniforme por evento**. No afirmar que la muestra es estratificada; lo es uniforme por evento (de ahí D1).

**A2 — Lógica correcta, solo requiere la muestra nueva (+ refinamiento opcional):**
- 4.5 Abandono de carrito: la resta de conjuntos de sesiones es correcta; dará cifra real con datos intactos. **Confirmado el daño del muestreo:** el código imprime 99.0% de abandono porque, al caer eventos sueltos, muchas sesiones conservan solo el `cart` y pierden su `purchase`. Opcional: nivel producto. Resolver la triple contradicción de redacción (ver tabla §3).
- 4.6 Recurrencia: `groupby('user_id')` correcto; válido con muestra por usuario. Hoy da 97.1% one-time (8.102 vs 238), inflado por el muestreo + ventana solo-octubre.
- 4.7 Velocidad de decisión: válido con sesiones intactas. **Evidencia directa del daño:** el propio notebook descarta 185 de 674 sesiones (27%) por tiempos negativos, que llama "artefactos de la muestra aleatoria"; la mediana de 5.3 min vive sobre 489 sesiones contaminadas. Opcional: medir por producto (first view de P → purchase de P) en vez de first view de la sesión.
- 4.2 Precio vs conversión (Hallazgo 2): el scatter incluye categorías con muy pocas vistas (stationery 72, country_yard 174, medicine 184) porque el `join` usó la tabla sin filtrar; el funnel sí exigía ≥500 vistas. Aplicar el mismo umbral ≥500 para un read limpio. La conclusión cualitativa ("el precio no es el freno") probablemente aguanta, pero las cifras dependen del fix A1.

**A3 — Sin cambios, solo re-ejecutar:**
- 4.3 y 4.4 Patrones temporales: distribuciones marginales de eventos `purchase`; correctas.
- Limpieza (duplicados, precio=0) y feature engineering (hour, day_of_week, date, category_main): re-aplicar sobre la nueva muestra.

**A4 — Caveats transversales a dejar escritos en el notebook:**
- Ventana = solo octubre (un "one-time" podría comprar en noviembre).
- "first view → first purchase" puede cruzar productos distintos en la misma sesión.
- 31.8% de eventos sin `category_main` se excluyen del análisis por categoría.

**A5 — Cosmético:**
- Subtítulos mal numerados: los `suptitle` dicen "5.3 Patron Temporal" y "5.4 Patron de Compras por Hora" en secciones que son 4.3 y 4.4 (confirmado).

---

## 6. Próxima acción (tareas abiertas)
- [x] Revisar todos los insumos del proyecto, incluido el notebook del EDA (hecho 2026-05-28).
- [x] Definir número de usuarios a muestrear: contados 3.022.290 usuarios / 42.448.764 eventos en el crudo (14.05 ev/usuario) → n_users=71.199 calibrado para ~1M eventos (hecho 2026-05-28).
- [x] Ejecutar Fase 0 (re-muestreo) y guardar `muestra_usuarios.parquet` (semilla fija documentada) (hecho 2026-05-28).
- [x] Implementar A1 (funnel por unidad) al re-ejecutar el EDA (hecho 2026-05-29, `src/funnel.py` + 4.1).
- [x] Adaptar 4.2 (precio vs conversión) a `cat_funnel`, aplicar umbral ≥500 unidades, recalcular Hallazgo 2 (hecho 2026-05-29).
- [x] Re-ejecutar 4.3/4.4 (temporal); rediseñar gráfico horario (normalizado + intensidad) y enriquecer Hallazgo 3 con la lectura de intensidad (hecho 2026-05-29).
- [x] Recalcular 4.5 abandono por unidad (resuelve triple contradicción → 32.4%) (hecho 2026-05-29).
- [x] Recalcular 4.6 recurrencia por ocasión (flip mayor: 31.7% recurrentes, 69% del revenue) (hecho 2026-05-29).
- [x] Recalcular 4.7 velocidad por producto-en-sesión (mediana 2.2 min; negativos 27% → 0.1%) (hecho 2026-05-29).
- [x] Añadir 4.8 análisis de oportunidad (premio en $, timing retención, marca, Pareto) (hecho 2026-05-29).
- [x] Reescribir la síntesis (ahora 4.9) y fijar el mensaje central D2 (dos palancas) (hecho 2026-05-29).
- [x] Producir tabla comparativa de los 7 hallazgos (original vs nuevo) — sección 4.10, ampliada con los 4 análisis de 4.8 (hecho 2026-05-29).

**Abiertas (Fase II en adelante):**
- [x] **Quick-win Fase I (rúbrica):** hecho 2026-05-29. Celda 2 "Pregunta de Negocio" ampliada con tabla **PN1–PN3** (cada PN apunta a las secciones que la responden); nueva celda 3 **"Hallazgo central"** con el mensaje D2 (dos momentos, ambos en electrónica) y cifras duras. Ver §7.
- [x] **Fase II — héroe A (borrador):** comparativa exploratorio→aclaratorio del revenue en juego de electronics (4.8a) + justificación en el notebook (FASE II), hecho 2026-05-29. Ver §7.
- [ ] **Fase II — refinar héroe A (EQUIPO):** pulir estética siguiendo el taller (tipografía/paleta, posición de anotaciones, posible *callout* de número grande); idealmente **migrar a Plotly** para alimentar el dashboard. Callout de tareas dejado en el notebook (fin de FASE II).
- [x] **Fase II — héroe B Palanca B (borrador):** comparativa exploratorio→aclaratorio de la retención (2 barras 100% apiladas Compradores vs Revenue: 31,7% recurrentes = 69% del revenue, base común + flecha "el mismo grupo" + caja de acción con timing 1,8 días/85,5% misma categoría → nudge 24–72 h) + justificación. Guarda `aclaratorio_02_retencion_recurrentes.png`. Sintaxis verificada (sin ejecutar). Hecho 2026-05-29. Ver §7.
- [ ] **Fase II — aclaratoria PN3 (D5, EQUIPO):** comparativa exploratorio→aclaratorio de la **intensidad horaria** (4.3: la mañana convierte ~2× por visita). Pendiente.
- [x] **Fase III — Dashboard Streamlit (implementado):** `src/prep.py` (carga+limpieza+features, 980.057 filas), `src/metrics.py` (8 métricas portadas del notebook), `app/theme.py`, `app/charts.py` (10 figuras Plotly nativas), `app/app.py` (titular fijo + KPIs reactivos + 4 pestañas CHTA + sidebar con filtros). Cifras verificadas vs notebook. Hecho 2026-05-29. Ver §7.
- [x] **Fase III — desplegada** en Streamlit Community Cloud (`main` de `origin`, entry `app/app.py`). Hecho 2026-05-29. Ver §7.
- [x] **Fase III — refactor de memoria (agregados precomputados):** `src/build_aggregates.py` genera 5 parquets pequeños (~3,9 MB); el app los carga vía `src/agg_metrics.py` sin tocar el clickstream crudo; pico de RAM de >1 GB a ~194 MB. Cifras verificadas 45/45 vs notebook (`src/verify_aggregates.py`). Hecho 2026-05-29. Ver §7.
- [x] **Fase III — rediseño BI "E-commerce SA":** sistema de diseño (`app/theme.py` tokens + nuevo `app/ui.py`), barra de marca, navegación prominente, héroe+soporte por pestaña, ayudas no-gráfico (banner/KPI cards/insight cards/glosario/callouts), disciplina de un solo acento rojo (sin verde/azul, marcas en gris/rojo), y cross-filter estilo Power BI sobre los agregados. 45/45 + pico ~214 MB; `AppTest` sin excepciones. Hecho 2026-05-29. Ver §7.
- [ ] **Fase III — verificación visual del usuario:** ejecutar `.venv\Scripts\python.exe -m streamlit run app/app.py`, validar el test de 30 s, la navegación, el cross-filter (clic en categoría → filtra el detalle + chip/limpiar) y la reactividad de filtros.
- [ ] Pitch 3 min + coherencia del mensaje.

---

## 7. Bitácora de avance
- **2026-05-28** — Planeación. Confirmado que la muestra original es por evento y rompe sesiones; decidido re-muestrear por usuario (D1). Revisado el código del EDA: identificado que el funnel (4.1) requiere reescritura metodológica además del re-muestreo (A1). Creados los 3 documentos del proyecto. Pendiente: ejecutar Fase 0.
- **2026-05-28** — **Fase 0 ejecutada** (`src/sampling.py`, DuckDB streaming, semilla 42). Conteo del crudo: 42.448.764 eventos / 3.022.290 usuarios (14.05 ev/usuario) → n_users calibrado=71.199 para target ≈1M eventos. Muestra por usuario resultante: **982.106 eventos**, **71.199 usuarios**, **214.759 sesiones**; distribución event_type: view 943.903 (96.11%), cart 21.278 (2.17%), purchase 16.925 (1.72%). Guardada en `data/processed/muestra_usuarios.parquet` (28.1 MB, no versionada). Nota operativa: el script imprime emojis y falló al inicio por la consola Windows (cp1252); se re-ejecutó con `PYTHONUTF8=1`. Próximo: Fase 1 (re-ejecutar EDA sobre esta muestra + implementar A1). Entorno en venv propio `.venv/` (Python 3.14.5).
- **2026-05-28** — Revisión completa de insumos (incl. notebook `taller_2.pdf`, export HTML de 28 págs). Confirmados todos los diagnósticos previos y añadido detalle: (a) el muestreo ejecutado fue `USING SAMPLE 500000` uniforme por evento, distinto del estratificado que sugería un comentario (A1-bis); (b) A1 verificado en el código (cart_rate y conv_rate cuelgan de views, sin encadenar); (c) Hallazgo 5 tiene triple contradicción real (99.0% / 79% / 79%) y el 99% está inflado por el muestreo; (d) Hallazgo 7 — el equipo ya descartó 27% de sesiones por tiempos negativos ("artefactos de la muestra"), evidencia directa del daño; (e) Hallazgo 2 — el scatter incluye categorías con <500 vistas que deben filtrarse; (f) `02_Contendio_curso_sum.md` es duplicado exacto de `Contexto_curado`; (g) guardado el FILE_ID del crudo y el volumen de referencia de la muestra actual. Actualizado este documento para que al iniciar cada chat se lean TODOS los insumos. Próximo paso sin cambios: Fase 0.
- **2026-05-29** — **Fase 1 en progreso sobre la muestra por usuario.** Hechas y corregidas las secciones del notebook: (a) **narrativa de muestreo** (celdas 3–11) documentando el cambio por-evento→por-usuario; (b) **carga** (celda 12) lee el parquet vía `config.py`, normaliza `event_time` a datetime; (c) **calidad+limpieza** (1–3): hardcoded reemplazados por cálculo dinámico — nulos `category_code` 31.98%, `brand` 14.38%, duplicados 564 (0.06%); gráficas de calidad re-etiquetadas a "nivel evento" y precio cambiado a **por producto único** (decisión metodológica: ver memoria del proyecto). (d) **A1 implementado**: `src/funnel.py` (`global_funnel`, `category_funnel`) con unidad = **producto dentro de sesión**, clasificación por etapa más profunda y tasas encadenadas; usado en 4.1. **Resultados del funnel por unidad:** 642.169 unidades; cart_rate 3.61%, conv_rate 2.44%, cart→purchase 67.6%; partición 96.39% solo-vista / 1.17% carrito-sin-compra / 2.44% compra. Por categoría (conv_rate): electronics 3.92% (cart 6.12, cierre 64%), appliances 2.38%, computers 1.53% (peor cierre 61.3%), apparel 0.52% (cierre 100% → freno upstream/vitrina). **Giro de mensaje:** el "persuadible" (carrito abandonado) se concentra en electronics (~5.048 unid, ~67% del total) y appliances, NO en construction (~70). El slopegraph (celda 28) pasó a dinámico desde `cat_funnel`, protagonista=electronics **provisional** (variable `HIGHLIGHT`). Pendiente al retomar: **4.2** (adaptar a `cat_funnel`, umbral ≥500, Hallazgo 2). Nota operativa de flujo: como el notebook con outputs supera el límite de lectura, antes de cada tanda de ediciones se corre `nbstripout` y el usuario re-ejecuta.
- **2026-05-29** — **4.2 Precio vs conversión (Hallazgo 2) recalculado.** Celda `precio_cat`: el precio ahora es **por producto único** (`groupby(['category_main','product_id']).price.median()` y luego agregado por categoría), consistente con la decisión metodológica y la gráfica de calidad; antes promediaba `price` por evento. Celda de cruce: reemplazada `conv_df` (inexistente) por **`cat_funnel`** (funnel por unidad de 4.1); el umbral **≥500 unidades sale solo** porque `cat_funnel` ya viene filtrado y el `join` es `inner`. **Resultado (10 categorías ≥500 unid):** no hay tendencia precio↔conversión; las más baratas convierten **peor** —apparel ($68 mediana → 0.52%), accessories ($55 → 0.70%)— y la mejor es **electronics 3.92%** a precio intermedio ($152); la más cara (sport $257) convierte 0.99%. Boxplot: comprados (mediana $179) no son más baratos que vistos ($162). Mediana de conversión entre categorías ≈1.55%. **Conclusión "el precio no es el freno" se mantiene y se refuerza.** Markdown del Hallazgo 2 reescrito con cifras reales y patrón Contexto→Hallazgo→Traducción→Acción. (Edición de markdown hecha sobre copia `.json` para preservar los outputs ya ejecutados, sin stripear.) Pendiente: 4.3/4.4 (temporales) y 4.5–4.7 (sesión/usuario).
- **2026-05-29** — **4.3/4.4 (patrón temporal) re-ejecutados y enriquecidos.** Fix cosmético A5: `suptitle` "5.3"/"5.4" → "4.3"/"4.4". **Hallazgo 3:** pico real **7h** (no 9h), franja **6h–10h**; por día efecto modesto (mar–jue ~2.600–2.700 vs fin de semana ~2.170; promedio ≈2.418/día). **Rediseño del gráfico horario (celda 38):** las vistas (~96% de eventos) aplastaban a compras/carritos en eje compartido → ahora tabla `hora_tab` + 2 paneles (distribución **normalizada** por tipo, e **intensidad** = compras por 100 vistas, nivel evento). **Insight nuevo y fuerte:** tráfico (vistas) pica en la **tarde** (16h, 7.2%) pero las compras pican en la **mañana** (7h, 8.4%), y la mañana **convierte ~2× por visita** (7h: 2.71 vs 16h: 1.20) → la tarde es vitrineo de baja intención, la mañana es decisión. Esto afila la recomendación de incentivos (ROI por impacto mayor en la mañana) y conecta con el mensaje del usuario persuadible. **Hallazgo 4:** 4/5 categorías pican 6–7h (electronics/computers 7h, appliances/furniture 6h); **apparel** se desplaza al **mediodía (12h, 12% concentración)** —no vespertino 13h–18h como decía la muestra vieja— y **furniture** ahora muestra pico claro (antes "sin patrón"). Limitación actualizada: 982.106 eventos ≈ 2.3% de octubre. Todos los markdowns editados sobre copia `.json` preservando outputs. Pendiente: 4.5 abandono, 4.6 recurrencia, 4.7 velocidad.
- **2026-05-29** — **Higiene de figuras.** Los `plt.savefig` de 4.2–4.7 (6 celdas) usaban rutas relativas y guardaban los PNG en `notebooks/`; todos pasados a `config.FIGURES / '...'` (→ `outputs/figures/`), consistentes con calidad/funnel/slopegraph que ya lo hacían. Los 4 PNG sueltos en `notebooks/` se movieron a `outputs/figures/`. Ambas rutas ya estaban en `.gitignore` (figuras reproducibles, no versionadas).
- **2026-05-29** — **4.5 Abandono de carrito reescrito por unidad (Hallazgo 5).** El código viejo medía sesiones-con-cart menos sesiones-con-purchase (set difference), que daba 99% inflado por el muestreo por evento y además no encadenaba por producto. Reescrito reutilizando el funnel de 4.1 (`gf`, `cat_funnel`): titular = abandono por unidad (producto-en-sesión) + tabla y gráfico de barras de abandono por categoría (persuadible electronics/computers resaltado, anotado con volumen de carritos abandonados). Título de sección → "(por unidad)". **Resultados:** abandono global **32.4%** (7.511/23.174; cierre 67.6%) — **resuelve la triple contradicción** (99% era artefacto, 79% sin respaldo). Por categoría: computers 38.7% (solo 334 carritos), electronics 36.0% (**5.048 abandonados = 67% del total**), appliances 33.8% (823); electronics+appliances+computers = 83% del abandono. apparel 0% (154/154 cierran → freno aguas arriba, cart_rate 0.52%). **Define con datos al \"usuario persuadible\":** intención que no cierra, casi todo en electrónica/electrodomésticos; incentivo = recuperación de carrito (no precio), en franja matutina (H3). Corregida también la frase del Hallazgo 5 en la síntesis 4.8 (79% → 32%). Markdowns editados sobre copia `.json`. Pendiente: 4.6 recurrencia, 4.7 velocidad.
- **2026-05-29** — **4.6 Recurrencia reescrita por ocasión (Hallazgo 6) — FLIP MAYOR.** Decisión metodológica: recurrencia = nº de **sesiones distintas con compra** (ocasiones), no eventos purchase (un carrito multi-ítem no es "recurrente"). Celdas 47+48 consolidadas (47 calcula `user_buys` una vez; 48 reutiliza), título con la definición. **Resultado que revierte el hallazgo viejo:** one-time **68.3%** (5.492) y **recurrentes 31.7%** (2.544 de 8.036) — el "97.1% one-time" era artefacto del muestreo por evento (compras sueltas → todos parecían únicos); confirma el valor de D1. El núcleo recurrente concentra el **69% del revenue**, ticket promedio **$1.411 vs $293** (~5×); cola larga hasta 44 ocasiones. **Implicación estratégica:** aparece una palanca de **retención** complementaria a la del persuadible → revisar el mensaje central (D2) en Fase 2/3. Caveat ventana solo-octubre (31.7% es piso). Sintetizada también la frase del Hallazgo 6 en 4.8. Pendiente: 4.7 velocidad.
- **2026-05-29** — **4.7 Velocidad de decisión reescrita por producto-en-sesión (Hallazgo 7) — FASE 1 COMPLETA.** Decisión metodológica: medir el tiempo del primer `view` del producto **comprado** a su compra en la misma sesión (no first-view→first-purchase de la sesión, que cruzaba productos, A4). Celdas 50 (intro+nota), 51 (cálculo) y 52 (etiquetas) actualizadas; conteo de negativos ahora dinámico. **Resultados:** mediana **2.2 min** (antes 5.3), 77.8% <5min, 91.3% <10min, ~1.7% >30min. **Negativos: 0.1% (20/15.627) vs 27% antes** → confirma que eran daño del muestreo por evento, no del fenómeno; muestra válida 489→15.607 unidades (32×). El comprador con intención activa decide en minutos → incentivo inmediato/en pantalla; los canales diferidos quedan para abandono (H5) y recurrencia (H6). Corregida la frase del Hallazgo 7 en la síntesis 4.8. **Con esto los 7 hallazgos están recalculados sobre la muestra por usuario.** Próximo: revisión completa de la síntesis 4.8 + mensaje central (D2), y tabla comparativa original vs nuevo (Fase 2).
- **2026-05-29** — **Añadida sección 4.8 "¿Dónde está el dinero?" (4 análisis de oportunidad) y reescrita la síntesis (renumerada a 4.9).** Cuatro celdas nuevas insertadas antes de la síntesis (ids 80–88; notebook ahora 67 celdas): **(a) Premio en $** — $2,53 M en juego en carritos abandonados (≈mitad del revenue capturado), 82% en electronics ($2,08 M); recuperar 10% en electronics = $207.700; brecha one-time→recurrente $1.117/usuario. **(b) Timing de retención** — 2ª compra mediana 1.8 días, 75% en 1ª semana, 85.5% misma categoría → nudge 24–72 h. **(c) Marca en electronics** — Samsung+Apple = 70% de carritos; Apple ticket $732 concentra ~½ del revenue abandonado. **(d) Pareto** — 8% de productos = 80% del revenue; electronics = 78%, top-3 = 88%. **Síntesis 4.9 reescrita** integrando todo y corrigiendo cifras viejas (conv global 1.8%→2.44%, electronics 2.72%→3.92%, narrativa construction→electronics, apparel vespertino→mediodía). **Mensaje central D2 fijado: dos palancas en paralelo** (conversión del persuadible + retención del núcleo recurrente), ambas en electrónica. Todas las figuras nuevas en `outputs/figures/` (exploracion_08..11). Próximo: tabla comparativa de hallazgos y Fase II (aclaratorio) / Fase III (dashboard).
- **2026-05-29** — **Tabla comparativa 4.10 (original vs corregido).** Añadida en la celda markdown vacía entre la síntesis 4.9 y el encabezado FASE II: tabla de los 7 hallazgos (valor por-evento vs por-usuario + por qué cambió) y lectura de cierre (3/4 robustos, 5/6/7 cambios sustanciales que validan D1). Nota operativa: **`nbstripout` reasigna los `id` de celda** (ahora secuenciales), así que para editar hay que localizar celdas por contenido, no por id viejo. Commit previo b03fc42.
- **2026-05-29** — **Tabla 4.10 ampliada y EDA cerrado.** Añadido a 4.10 un segundo bloque con los 4 análisis de oportunidad de 4.8 (premio en $, timing, marca, Pareto) y su "para qué decisión". Tablero: Fases 2 (validar) y 3 (mensaje central) marcadas ✅; **Fase II (aclaratorio) queda como siguiente**. Con esto la Fase I / EDA está completa y commiteada. El notebook tiene 67 celdas (4.1–4.10 + síntesis); estructura de secciones estable. Próximo chat: arrancar Fase II (transición exploratorio→aclaratorio del gráfico principal).
- **2026-05-29** — **Fase II — borrador del héroe B (Palanca B / retención).** Insertadas 4 celdas en FASE II (tras el héroe A, antes del callout): intro (PN2), **ANTES** (exploratoria, reproduce los 2 gráficos de 4.6), **DESPUÉS** (aclaratoria: **dos barras 100% apiladas** Compradores vs Revenue con base común, recurrentes resaltados, etiquetas % directas sin eje Y/spines, flecha "el mismo grupo" conectando 31,7%→69%, y **caja de acción** con el timing 1,8 días / 85,5% misma categoría → nudge 24–72 h; guarda `aclaratorio_02_retencion_recurrentes.png`) y **justificación** (tabla decisión→principio + CHTA + 30 s). Reusa variables de 4.6 (`one_time/repeat/n_buyers/rev_*`) y 4.8b (`days/same`). Cuidado tomado: sin pares `$...$` en texto matplotlib (evita mathtext); montos en "USD". Callout de tareas actualizado (B → Borrador; queda PN3). Sintaxis de ambas celdas verificada (sin ejecutar el notebook). Pendiente: que un compañero ejecute y refine; aclaratoria de PN3.
- **2026-05-29** — **Fase II arrancada — borrador del héroe A + decisión D5.** En la sección FASE II del notebook se insertaron 4 celdas: intro (PN1 + por qué este héroe), **ANTES** (versión exploratoria, reproduce 4.8a), **DESPUÉS** (versión aclaratoria: título-acción, electronics resaltado, resto colapsado, etiquetas $ directas sin eje X/spines, callout "82% del premio", anotación-flecha al escenario de recuperación; guarda `aclaratorio_01_revenue_electronics.png`) y **justificación** (tabla decisión→principio del curso + patrón CHTA + prueba 30 s). Composición elegida = **opción A** (barras + categoría resaltada + anotación), que es la que recomienda el material del curso (§5 mapa pregunta→gráfico: "barras, color fuerte solo a la categoría crítica"; §1 contexto indispensable; §3.1 Data-to-Ink/regla de los segundos). Sintaxis de ambas celdas de código verificada (sin ejecutar el notebook completo). **El usuario decidió no pulir aquí:** el borrador queda como **andamiaje para el equipo** y se fija **D5** (no limitarnos a un solo gráfico → aclaratorias también de Palanca B y PN3). Añadido un callout de "tareas pendientes para el equipo" al final de FASE II. Pendiente: que un compañero ejecute y refine el héroe, y producir las aclaratorias adicionales.
- **2026-05-29** — **Quick-win Fase I aplicado (rúbrica).** Celda 2 "Pregunta de Negocio": conservada la pregunta paraguas y añadida tabla con las **tres PN explícitas** (PN1 dónde/cuánto vale; PN2 quiénes valiosos/retención; PN3 cuándo/con qué), cada una mapeada a las secciones que la responden. Nueva celda 3 **"Hallazgo central"** (justo tras la pregunta, estilo resumen ejecutivo) con el mensaje D2: *"el negocio deja dinero sobre la mesa en electrónica"*, dos momentos —(A) recuperar carritos abandonados (32,4% abandono, electronics 67%, ~$2,08 M, Apple/Samsung, decisión en 2,2 min, incentivo inmediato matutino) y (B) retener recurrentes (31,7% = 69% del revenue, recompra 1,8 días, 85,5% misma categoría, nudge 24–72 h)—. Editado con `nbformat` (mismo formato que `nbstripout` → diff acotado a 2 celdas); `$` escapados como `\$` para que Jupyter no los lea como MathJax. Pendiente del usuario: renderizar y revisar. Próximo: Fase II (gráfico héroe).
- **2026-05-29** — **Estrategia de Fase II persistida (alineación con la rúbrica).** Fijadas las **preguntas de negocio PN1–PN3** (§1) y el refinamiento de D2 a *"una historia, dos momentos"* para no diluir el test de 30 s. Nueva decisión **D4**: gráfico héroe de Fase II = Palanca A monetizada (revenue en juego/abandono de electronics, 4.8a); entregable = comparativa exploratorio→aclaratorio + justificación. Añadido quick-win de Fase I (celda con pregunta de negocio + hallazgo central, que la rúbrica evalúa). §6 actualizada con el orden: quick-win → Fase II → Fase III. Sin cambios en el notebook este paso.
- **2026-05-29** — **Fase III desplegada + proyecto movido al repo propio del usuario.** Streamlit Cloud requería permisos sobre el repo (el del equipo es de Heider y el usuario no es admin), así que se movió el proyecto al repo personal **`ylondonq/taller_visualiza_datos-`**. Reconfiguración de remotos: `origin` → repo del usuario; el del equipo (`HeiderZapata/taller_visualiza_datos`) quedó como remoto **`equipo`** (para unir a los compañeros más adelante vía PR). Se versionó `muestra_usuarios.parquet` (28 MB, excepción documentada en `.gitignore`). El push de `main` al repo propio se hizo con `--force` (el repo se creó con un commit autogenerado). **Dashboard EN VIVO: https://taller02dataviz.streamlit.app/** (rama `main`, entry `app/app.py`); redepliega solo en cada `git push origin main`. Nota: GitHub avisó (warning, no error) de un `raw/muestra_eventos_def.csv` de ~62 MB en el historial (muestra vieja, ya no se usa) — limpieza opcional futura. **Pendiente:** iterar el diseño con el feedback del equipo (al usuario no le convenció el resultado inicial). Nota de seguridad: el push a un remoto externo nuevo lo bloquea el clasificador de Claude (parece exfiltración); lo ejecuta el usuario con `! git push`.
- **2026-05-29** — **Fase III implementada localmente (reconstrucción, Opción B).** La Fase III se había implementado en una sesión de Claude Code en la nube (rama `Yeison`, 5 commits) pero no se pudo pushear (sin credenciales en el contenedor) y el `fase3-dashboard.bundle` no apareció en disco; por eso se **reconstruyó localmente** desde la especificación del usuario. Creados, sin tocar `src/funnel.py` ni `config.py`: **`src/prep.py`** (`load_sample`: porta carga+limpieza+features del notebook — `event_time` tz-naive, `drop_duplicates`, `price>0`, `hour/day_of_week/date/category_main` → **980.057 filas**); **`src/metrics.py`** (8 funciones puras con fórmulas idénticas al notebook: `recurrence` 4.6, `abandonment` 4.5, `revenue_at_stake` 4.8a, `repurchase_timing` 4.8b, `brand_mix` 4.8c, `hourly_intensity` 4.3, `decision_speed` 4.7, `price_vs_conversion` 4.2; reusa `funnel.py`); **`app/theme.py`** (plantilla Plotly gris/rojo + `fmt_money/pct/int`); **`app/charts.py`** (10 figuras Plotly nativas, incluidos los héroes A y B); **`app/app.py`** (`layout=wide`, carga `@st.cache_data`, **titular-acción fijo** con cifras ancla sobre el df sin filtrar, **fila de KPIs reactivos**, **4 pestañas** Resumen/A/B/Cuándo cada una con bloque Contexto→Hallazgo→Traducción→Acción, **sidebar** categoría+hora primarios y marca+segmento en `expander` + caveat A4). **Cifras verificadas vs notebook**: abandono 32,4%, conv 2,44%, recurrentes 31,7%/69%, ticket $293/$1.411, revenue en juego $2.535.264 (electronics $2.08 M), decisión 2,2 min, recompra 1,8 d / 85,5% misma categoría, pico intensidad 7h. Las 10 figuras serializan (`to_json`) y todos los módulos compilan. Commits en `Yeison`: capa de datos (`f63446c`) + capa visual (`36b2556`). **Pendiente del usuario:** ejecutar `streamlit run app/app.py` (test de 30 s + reactividad) y `git push`.
- **2026-05-29** — **Git: push de Yeison (con outputs) + main reemplazado; transición a Fase III.** El usuario ejecutó el notebook completo y se commiteó **con outputs** (excepción puntual a la regla de `nbstripout`, ~1,7 MB) para visibilidad del equipo en GitHub; push de `Yeison` (b00e876→312ba03). Por decisión explícita del usuario (con backup previo en otra rama), `origin/main` se **forzó a Yeison** (0dd62fa→312ba03): `main`, `Yeison` y sus remotos quedan en el mismo commit y en la **estructura nueva** (notebook en `notebooks/taller_2.ipynb`). ⚠️ Los 2 commits que tenía `main` (aclaratoria de hallazgos 1 y 7 de un compañero: `0dd62fa`, `f3d5f9e`) salieron de `main` → **pendiente:** confirmar que el backup esté pusheado al remoto y **avisar al equipo** del cambio de estructura y del reemplazo de main. **Próximo: Fase III** (dashboard Streamlit `app/app.py`). Preparado un prompt de continuación: empezar por el plan (arquitectura de `app.py`, layout KPIs/paneles/filtros, mapeo PN→gráfico→acción) antes de codificar; reutilizar `config.py`, `muestra_usuarios.parquet` y `src/funnel.py`; valorar Plotly; **volver a limpiar outputs con `nbstripout`** (el commit con outputs fue excepción puntual).
- **2026-05-29** — **Refactor de memoria del dashboard (agregados precomputados).** Streamlit Cloud tumbaba la app con *"over its resource limits / using too much memory"*: `src/prep.py` cargaba el clickstream entero (980.057 filas = **237,7 MB** en RAM solo el DataFrame, por las cadenas `user_session`/object) y `app.py` derivaba TODAS las métricas al vuelo sobre él — `revenue_at_stake` hace `df.copy()` (+238 MB), `decision_speed` ordena las 980k filas, y se calculaba sobre el df completo (anclas) **y** sobre el filtrado (KPIs) → pico >1 GB. **Solución:** nuevo `src/build_aggregates.py` lee la muestra UNA vez (misma limpieza/features de `prep.py` y mismas fórmulas de `funnel.py`/`metrics.py`) y la colapsa a 5 parquets pequeños con dtype `category`: **`agg_units.parquet`** (642.169 unidades session×product: banderas funnel, category/brand, price mediana, hora del 1er evento, minutos de decisión, segmento), **`agg_occasions.parquet`** (14.374 ocasiones de compra: revenue/ítems/categoría-marca dominante/instante/segmento), **`agg_hourly.parquet`** (1.776 filas hora×event_type×categoría×segmento), **`agg_price_cat.parquet`** (13 cats, precio por producto único) y **`agg_anchors.parquet`** (cifras fijas del titular). Nuevo `src/agg_metrics.py` reimplementa cada métrica con la MISMA fórmula pero desde estos agregados, devolviendo estructuras idénticas → **`app/charts.py` no se tocó**. `app.py` ahora carga solo los agregados (`@st.cache_data`) y los filtra a su grano (categoría/hora/marca/segmento); ya no importa `prep`/`metrics`. **Pesos:** 5 parquets = **3,90 MB en disco** (vs 28 MB de la muestra) / ~11 MB en RAM. **Pico de RAM del app entero: ~194 MB** (134 MB son las librerías pandas+plotly+streamlit; datos+métricas+10 figuras suman ~60 MB) → holgado bajo el ~1 GB del tier gratuito. **Verificación (`src/verify_aggregates.py`): 45/45 cifras idénticas** a `metrics.py` sobre el clickstream completo (= notebook): abandono 32,41%, conv 2,44%, recurrentes 31,66%/69,01%, ticket $293/$1.411, revenue en juego $2.535.264 (electronics $2,077 M), decisión 2,17 min, recompra 1,79 d / 85,5% misma categoría, pico intensidad 7h. **Caveats de fidelidad de los filtros** (el caso SIN filtros es exacto): el filtro de hora en métricas de unidad corta por la hora del primer evento de la unidad (no trocea eventos sueltos); el filtro de marca no afecta a la figura de intensidad horaria (2.572 marcas dispararían ese agregado); recurrencia/timing se filtran por la categoría/marca dominante de cada ocasión. Los 5 parquets se versionan (excepción en `.gitignore`, como la muestra) para que la nube los tenga. Pendiente: verificación visual del usuario (`streamlit run app/app.py`).
- **2026-05-29** — **Rediseño de presentación del dashboard ("E-commerce SA"), guiado por `PROMPT_ClaudeCode_rediseno_dashboard.md`.** No cambia datos ni cifras: es capa visual/UX siguiendo los principios del curso (`Contexto_Taller_Visualizacion_curado.md`). **Qué se hizo:** (1) **Sistema de diseño** en `app/theme.py` — tokens centralizados (`BRAND_RED` único acento, escala `GRAY_900..100`, `BG/SURFACE/TEXT/MUTED`, stack tipográfico, tamaños), plantilla Plotly en grises+rojo con fondo transparente; **regla de color documentada** (color saturado solo para lo importante; resto gris; §3.2/§3.4). (2) Nuevo **`app/ui.py`** con componentes y CSS global: `header_bar` (marca + logo CSS "E" sin imágenes + chip "Datos: Oct 2019"), `kpi_card` (con `accent` para el ancla), `insight_card` (CHTA con iconos, no párrafo), `action_banner` (acto de habla "motivar", §3.5), `section_header`, `callout` (número grande), `filter_chip`+limpiar, `glossary_expander` (abandono/conversión/recurrente/revenue en juego/nudge para el neófito), y `<style>` que estiliza las pestañas (grandes, activo con subrayado rojo grueso) y oculta el chrome de Streamlit. (3) **`app/charts.py`** — disciplina de un solo acento: rojo solo en el elemento a resaltar, contexto en grises por luminosidad; **eliminado el verde de la gráfica de marcas** (ahora gris=comprados, rojo=abandonados; un solo color semántico = "dinero que se pierde"); funnel/horario/decisión pasan de azul/verde a grises+rojo; `customdata` con la categoría en las 3 figuras seleccionables. (4) **`app/app.py`** reestructurado: marca arriba, navegación prominente (`Resumen` / `Palanca A · Conversión` / `Palanca B · Retención` / `Cuándo activar`), **jerarquía héroe+soporte** (una gráfica grande por pestaña; el resto en `st.expander("Ver detalle analítico")`), Resumen estratégico sin interacción (banner + 5 KPI cards con ancla en rojo + héroe revenue + callout $2,08 M + glosario; test de 30 s), y cada pestaña analítica abre con `insight_card` CHTA + `action_banner`. (5) **Cross-filter estilo Power BI** sobre los agregados cacheados (no el crudo): `st.plotly_chart(on_select="rerun")` en el héroe de Palanca A (clic en categoría → filtra funnel y marcas del detalle) y en el scatter de "Cuándo activar" (clic en categoría → enfoca intensidad horaria y velocidad); `chip` de filtro activo + botón **Limpiar selección** (implementado con un *nonce* en `session_state` que recrea el widget para resetear la selección, evitando bucles de rerun); Palanca B usa el **segmento** del sidebar como control (la retención es a nivel comprador, no por categoría) — documentado. (6) `requirements.txt`: `streamlit>=1.35` (necesario para `on_select`). **Caveats de fidelidad** documentados en código (hora = primer evento de la unidad; marca no alimenta el horario; recurrencia/timing por categoría dominante). **Verificación:** `src/verify_aggregates.py` sigue **45/45**; las 10 figuras serializan; los 5 módulos compilan; el app NO importa `prep`/`metrics`; **`streamlit.testing.AppTest` ejecuta el script sin excepciones** (4 pestañas, y un cambio de segmento a "Recurrentes" re-ejecuta sin error); **pico de RAM ~214 MB (<250)**. Pendiente: verificación visual del usuario y feedback del equipo sobre la estética.
